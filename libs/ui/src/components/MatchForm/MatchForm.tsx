import React, { FC } from 'react';


interface MatchFormProps {}

const MatchForm: FC<MatchFormProps> = () => (
  <div data-testid="MatchForm" className=''>
    Déclarer les résultats d'un match
  </div>
);

export default MatchForm;
